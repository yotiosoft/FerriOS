[workspace]
resolver = "3"
members = ["kernel", "user"]

[package]
name = "ferrios-runner"
version = "0.1.0"
edition = "2024"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[build-dependencies]
bootloader = { version = "0.11", features = ["bios"] }
